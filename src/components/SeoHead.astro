---
// SeoHead.astro
export interface Props {
  title?: string;
  description?: string;
  image?: string;
  canonicalUrl?: string;
  type?: 'website' | 'article';
  publishedTime?: string;
  tags?: string[];
}

const {
  title = 'INR Finance Compass — Sovereign, Private Personal Finance OS',
  description = 'Sovereign personal finance OS engineered for Indian realities. Zero surveillance, zero data harvesting, anchor-aware ledger integrity, and client-side statement parsing.',
  image = '/images/og-default.png',
  canonicalUrl,
  type = 'website',
  publishedTime,
  tags = [],
} = Astro.props;

const siteUrl = Astro.site ? Astro.site.toString().replace(/\/$/, '') : 'https://financecompass.in';
const currentUrl = canonicalUrl || new URL(Astro.url.pathname, Astro.site || 'https://financecompass.in').href;
const formattedTitle = title.includes('INR') ? title : `${title} | INR Finance Compass`;
const ogImageUrl = image.startsWith('http') ? image : `${siteUrl}${image.startsWith('/') ? '' : '/'}${image}`;
const rssUrl = new URL(`${import.meta.env.BASE_URL.replace(/\/$/, '')}/rss.xml`, Astro.site || 'https://financecompass.in').href;
---

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL.replace(/\/$/, '')}/favicon.svg`} />
  <meta name="generator" content={Astro.generator} />

  <!-- Primary Meta Tags -->
  <title>{formattedTitle}</title>
  <meta name="title" content={formattedTitle} />
  <meta name="description" content={description} />
  <meta name="author" content="INR Finance Compass" />
  {tags.length > 0 && <meta name="keywords" content={tags.join(', ')} />}

  <!-- Canonical URL -->
  <link rel="canonical" href={currentUrl} />

  <!-- RSS Feed Auto-Discovery -->
  <link rel="alternate" type="application/rss+xml" title="INR Finance Compass Blog" href={rssUrl} />

  <!-- Open Graph / Facebook -->
  <meta property="og:type" content={type} />
  <meta property="og:url" content={currentUrl} />
  <meta property="og:title" content={formattedTitle} />
  <meta property="og:description" content={description} />
  <meta property="og:image" content={ogImageUrl} />
  {publishedTime && <meta property="article:published_time" content={publishedTime} />}

  <!-- Twitter / X -->
  <meta property="twitter:card" content="summary_large_image" />
  <meta property="twitter:url" content={currentUrl} />
  <meta property="twitter:title" content={formattedTitle} />
  <meta property="twitter:description" content={description} />
  <meta property="twitter:image" content={ogImageUrl} />

  <!-- Theme Color and Script to prevent FOUC -->
  <meta name="color-scheme" content="light dark" />
  <meta name="theme-color" content="#059669" />

  <script is:inline>
    // Inline script immediately executed to prevent flash of wrong theme
    (function() {
      const stored = localStorage.getItem('theme');
      if (stored === 'dark' || (!stored && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        document.documentElement.classList.add('dark');
      } else {
        document.documentElement.classList.remove('dark');
      }
    })();
  </script>
</head>
